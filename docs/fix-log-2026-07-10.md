# Fix Log - 2026-07-10

## Summary

本次 UI 修复聚焦任务进度实时显示、历史事件恢复、前端长连接超时保护与若干 hydration/lint 问题。

## Commit

- `7136ddc` - `fix: update task progress panel in real time`

## Changes

- 历史事件归一化兼容 `{ event, data }` 与 `{ type, plan/step/message }` 两种后端事件结构。
- 任务进度面板在当前对话轮次中合并 `plan` 与 `step` 事件，避免任务结束后进度条从多条缩水成 1 条。
- `PlanPanel` 折叠态优先显示当前 running/pending 步骤，不再一直显示第一步。
- running 步骤计入当前进度，并显示加载图标，让执行中状态可见。
- 聊天 SSE 前端超时从 5 分钟缩短，避免连接长时间悬挂。
- 修复若干前端稳定性问题：
  - 固定 sidebar skeleton 宽度，避免 hydration 不一致。
  - 移除 session item 的 mounted 分支。
  - 调整设置页按钮文案，修复 lint 问题。

## Verification

- `npm run lint`: `0 errors, 21 warnings`
- `npm run build`: passed
